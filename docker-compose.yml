version: '3'
services:
  web:
    build: .
    command: mix phx.server  
    volumes:
      - .:/home/app
    ports:
      - "4000:4000"
    links:
      - db
    depends_on:
      - db

  # test:
  #   image: education_web
  #   command: mix test
  #   environment:
  #     MIX_ENV: test
  #     PORT: 4001
  #   volumes:
  #     - .:/home/app 

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - 54320:5432